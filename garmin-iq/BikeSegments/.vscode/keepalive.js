// Keepalive used by the "Build+Install+Run BikeSegments (Debug)" launch config.
console.log('BikeSegments keepalive started. PreLaunchTask will build and run the PRG.');
console.log('Press Stop in VS Code to end the session.');
// Keep process alive
setInterval(function() {}, 1000 * 60 * 60);
